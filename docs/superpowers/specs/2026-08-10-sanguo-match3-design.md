# 三国消消乐 — 微信小游戏设计文档

- 日期：2026-08-10
- 状态：已确认（待实现）
- 技术栈：原生微信小游戏（JavaScript + Canvas），零第三方依赖

---

## 1. 概述

一款以《三国演义》为题材的文字三消微信小游戏。核心特色：**基础字块三消** + **三国故事短语连字**双玩法。每通关一个故事关就解锁该故事的字块加入公共字池，字池随进度不断扩充；每个故事还附带一个初始技能和更强版的觉醒技能（精英模式解锁），形成轻度养成。

## 2. 核心玩法

### 2.1 基础操作：互换三消
- 8×8 棋盘，字块类型为基础字块（兵、弓、车、枪、骑）。
- 点击一个字块与相邻（上下左右）字块互换，横/竖 3 个及以上相同字即消除。
- 4 连、5 连、连锁消除有额外加分。
- 消除后字块下落，从当前字池随机补字。

### 2.2 进阶操作：故事短语连字
- 盘面上出现已解锁故事的字块（每盘随机挑 2~3 个已解锁故事上场）。
- 玩家一笔划过**相邻**字块，顺序恰好组成该故事短语 → 整组消除 + 触发该故事技能。

### 2.3 过关条件
- 限时内达到目标分即过关；时间耗尽未达标则失败。
- 失败可重试（再扣 5 体力）或退出回大厅。

## 3. 大厅与进度

- **大厅**只有一个「开始闯关」按钮：扣 5 体力 → 进入当前关。
- 界面**右上角**显示解锁预告，例如玩家在 2~4 关时显示「第 5 关解锁：温酒斩华雄」。
- 故事关 = 普通关 + 开局剧情简介 + 该故事字块出现率翻倍。故事关里当前故事的字块**必定上场**（其余按每盘 2~3 个上限随机补足）。

### 3.1 阶梯式解锁（三国时间线）

故事关解锁节奏：第 1、5、10 关各解锁一个，之后每 10 关解锁一个（1 → 5 → 10 → 20 → 30 → …），越到后面越稀疏，有递进感。

| 解锁关 | 故事 | 字块 | 剧情一句话 |
|-------|------|------|-----------|
| 1 | 桃园三结义 | 桃园三结义 | 刘备、关羽、张飞桃园结义 |
| 5 | 温酒斩华雄 | 温酒斩华雄 | 关羽温酒斩华雄 |
| 10 | 三英战吕布 | 三英战吕布 | 虎牢关三英合力 |
| 20 | 煮酒论英雄 | 煮酒论英雄 | 青梅煮酒论英雄 |
| 30 | 千里走单骑 | 千里走单骑 | 关羽千里寻兄 |
| 40 | 官渡之战 | 官渡之战 | 火烧乌巢定官渡 |
| 50 | 三顾茅庐 | 三顾茅庐 | 三顾茅庐请卧龙 |
| 60 | 草船借箭 | 草船借箭 | 草船借箭十万支 |
| 70 | 赤壁之战 | 赤壁之战 | 火烧赤壁定三分 |
| 80 | 华容道 | 华容道 | 关云长义释曹操 |
| 90 | 七擒孟获 | 七擒孟获 | 七擒七纵服南中 |
| 100 | 空城计 | 空城计 | 空城抚琴退司马 |

## 4. 数值配置（v1 初值，集中在 levels.js 可调）

| 项 | 数值 |
|----|------|
| 棋盘 | 8×8 |
| 基础字块 | 兵、弓、车、枪、骑（字池权重各 100） |
| 故事字块权重 | 每个已解锁故事字权重 15；每盘随机上 2~3 个已解锁故事 |
| 得分 | 3连=30、4连=60、5连=100；连锁每级 ×1.5；故事短语整组 = 500 |
| 普通关 | 时间 60 秒，目标分 = 800 +（关数-1）×150 |
| 精英关 | 时间 40 秒，目标分 = 普通目标 ×1.5 |
| 体力 | 初始 30 / 上限 30，每关 5 体力，失败重试再扣 5 |
| 体力恢复 | 1 体力 / 10 分钟（满 30 需 5 小时） |

## 5. 故事与技能系统

- **技能触发模型**：每个故事的初始技能靠划出该故事短语触发；觉醒技能为更强版本，通关对应精英关后**替换**初始技能（同一触发方式，效果更强，不叠加）。
- **技能表**（数据驱动，加技能只改 skills.js 配置）：

| 故事 | 初始技能 | 觉醒技能（精英解锁） |
|------|---------|--------------------|
| 桃园三结义 | 结义祝福：整组×3 + 10秒内消除×2 | 兄弟同心：整组×5 + 15秒内×3 |
| 温酒斩华雄 | 一刀两断：清除一整行 | 温酒未凉：清除整行+整列 |
| 三英战吕布 | 三英合力：随机 3 处三连消除 | 虎牢关决战：随机 6 处三连消除 |
| 煮酒论英雄 | 青梅煮酒：本关时间 +10 秒 | 论尽天下：时间 +20 秒并全盘刷新 |
| 千里走单骑 | 单骑奔袭：洗牌重排盘面 | 过五关斩六将：洗牌 + 生成一套「千里走单骑」字块 |
| 官渡之战 | 火烧乌巢：清除所有同一种基础字块 | 官渡决胜：清除所有两种基础字块 |
| 三顾茅庐 | 三顾之恩：立即生成一套「三顾茅庐」字块 | 卧龙出山：生成并自动触发一次 |
| 草船借箭 | 借箭：本关故事字块出现率翻倍 | 万箭齐发：清除盘面所有故事字块 |
| 赤壁之战 | 火烧赤壁：随机清除盘面 30% | 东风乍起：全屏清除 |
| 华容道 | 华容义释：时间暂停 5 秒 | 义释千古：暂停 8 秒 + 得分×2 |
| 七擒孟获 | 七擒七纵：随机 7 处小范围消除 | 南中归心：随机 10 处消除 |
| 空城计 | 空城退敌：剩余时间<10秒时消除×2 | 琴声退敌：立即全屏清除一次 |

## 6. 精英模式

- 每一关都有精英版，独立进度，从第 1 关开始。
- 精英关需先通关对应普通关才能玩。
- 规则：时间更短（40 秒）、目标分更高（×1.5）。
- 通关精英关 → 解锁该关故事的**觉醒技能**。

## 7. 广告与分享系统

### 7.1 激励视频广告
- **①补体力**：体力不足时看激励视频 +10 体力，每日限 5 次。
- **②失败续命**：时间耗尽失败时看激励视频续 15 秒，每关限 1 次。
- **③广告给道具**：看激励视频得 1 个随机道具，每日限 3 次。
- 技术：`wx.createRewardedVideoAd({adUnitId})`，广告位 ID 在 `ads.js` 配置（微信公众平台「流量主」开通后填入占位），需处理加载失败 / 拉取失败。

### 7.2 分享
- **分享给道具**：每日首次分享到微信群 / 好友得 1 个随机道具。
- 技术：`wx.shareAppMessage()`，分享后回调发放奖励，每日计数记录在存档。

## 8. 道具系统

| 道具 | 效果 |
|------|------|
| 铁锤 | 砸掉盘面任意一个字块 |
| 置换 | 任意两个字块互换位置 |
| 洗牌 | 重排整个盘面 |
| 加时 | +10 秒时间 |

- 闯关中点击道具图标消耗一个；库存存于存档。
- 获取渠道：分享给道具（每日首次）、广告给道具（每日 3 次）。

## 9. 技术架构（模块化）

```
main.js     入口 + 游戏循环 + 状态机(大厅/闯关中/结算)
  ├── board.js     棋盘生成、互换、消除、下落、补字
  ├── match3.js    三/四/五连判定、连锁
  ├── story.js     故事字块、一笔划线判定
  ├── skills.js    技能定义与执行（数据驱动）
  ├── levels.js    关卡配置表(目标分/时间/解锁/字池)
  ├── elite.js     精英模式规则
  ├── ui.js        Canvas 绘制(大厅/棋盘/右上角预告/计分)
  ├── ads.js       激励视频广告(补体力/续命/道具) 配置与封装
  └── storage.js   本地存档
```

- **数据流**：输入 → board 互换 → match3 判定 → 消除得分 → 下落补字 → 连锁 → UI 渲染。
- **状态机**：`LOBBY`（大厅）→ `PLAYING`（闯关中）→ `RESULT`（结算）；普通 / 精英两条进度轨道。
- **存档字段**（`wx.setStorageSync`）：`currentLevel`、`eliteLevel`、`stamina` + 上次恢复时间戳、`unlockedStories`（解锁/觉醒标记）、`props`（道具库存：铁锤/置换/洗牌/加时）、`dailyCounters`（每日广告/分享次数）、`highScores`、`settings`。

## 10. 错误处理

- 体力不足 → 提示 + 「看广告补体力」或「等待恢复」入口。
- 广告拉取 / 加载失败 → 隐藏广告按钮或提示「广告加载中，稍后再试」，不影响正常游戏流程。
- 棋盘无解（无可互换能形成三连）→ 自动检测并洗牌一次。
- 存档缺失 / 损坏 → 重置为初始状态并提示。
- 屏幕适配 → `wx.getSystemInfoSync()` 取屏幕尺寸动态计算格子大小。

## 11. 测试

- **纯函数单测**：三连判定、连锁、一笔划线路径校验、字池加权抽取——核心逻辑写成纯函数，Node 环境 `node test.js` 验证。
- **手动真机测试**：微信开发者工具 + 真机预览，走完「大厅→闯关→过关→解锁→精英→觉醒技能」全流程。

## 12. 后续扩展（非 v1）

- 广告补体力、内购。
- 云开发存档 / 排行榜。
- 更多三国故事（字池可持续扩充）。
- 音效、震动、粒子特效。
